# This file marks the "routes" directory as a Python package.
# In Python, an __init__.py file (even if empty) tells the interpreter that the directory should be treated as a package,
# allowing imports from submodules within this directory. For example, 'from routes import dashboard' would work if dashboard.py exists.
# This is essential for organizing Flask route handlers into a package that can be imported by the main app.py file.
