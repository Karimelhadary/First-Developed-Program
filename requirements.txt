# This file lists the Python packages required for the TaskManager application
# Install with: pip install -r requirements.txt

# Flask: Web framework for building the application
Flask>=2.3
# pymongo: MongoDB driver for Python, used for database operations
pymongo>=4.6
# dnspython: DNS toolkit, required for MongoDB Atlas connections
dnspython>=2.4
